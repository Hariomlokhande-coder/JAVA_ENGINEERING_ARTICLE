export const environment = {
  production: false,
  /** Base URL of the Spring Boot API. */
  apiUrl: 'http://localhost:8080/api',
  /** Origin that serves the uploaded images, used to resolve /uploads/... paths. */
  filesUrl: 'http://localhost:8080'
};
